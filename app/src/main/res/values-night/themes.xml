<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode -->
    <style name="Theme.Plm2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/blueDark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray</item>
        <item name="colorSecondaryVariant">@color/pink</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Стили для SwitchCompat -->
    <style name="SwitchCompatTheme.Light" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="thumbTint">@color/switchThumbColor</item>
        <item name="trackTint">@color/switchTrackColor</item>
    </style>

    <style name="SwitchCompatTheme.Dark" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="thumbTint">@color/DarkSwitchColorRe</item>
        <item name="trackTint">@color/switchTrackColor</item>
    </style>
</resources>
